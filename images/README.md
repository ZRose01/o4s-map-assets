# Event images

Drop event photos here (jpg/png/webp), then put the exact filename in
the `Image` column of the tracker CSV and re-run `build_events.py`.
Events without an image get a neutral placeholder automatically.

Displayed at 220×120 (popup) / ~300×180 (panel), so ~800px-wide,
web-compressed images are plenty.
